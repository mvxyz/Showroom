
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.ХарактеристикаНоменклатуры.Видимость = ЗначениеЗаполнено(Запись.ХарактеристикаНоменклатуры);
	Элементы.СерияНоменклатуры.Видимость = ЗначениеЗаполнено(Запись.СерияНоменклатуры);
	
	Заголовок = Запись.Склад.Наименование + ": остаток";
	
КонецПроцедуры

#КонецОбласти
